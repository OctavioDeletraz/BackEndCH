// import dotenv from 'dotenv';
// dotenv.config();

export const config = {
    client: 'mysql',
    connection: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'ecommerce',
    }
}